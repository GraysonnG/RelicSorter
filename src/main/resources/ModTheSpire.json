{
  "modid": "${project.artifactId}",
  "name": "${project.name}",
  "author_list": ["Blank The Evil"],
  "description": "",
  "dependencies" : ["basemod"],
  "version": "${project.version}",
  "sts_version": "${project.stsVersion}",
  "mts_version": "${project.mtsVersion}"
}